# Agent Instructions

- After writing any UI feature, run the frontend Biome check from `SporeSync.Web/ClientApp` with `npm run biome:check`.
- After writing any UI feature, run the UI test suite from `SporeSync.Web/ClientApp` with `npm test`.
- Report the commands run and whether they passed before handing work back.
