# Changelog

All notable changes to SporeSync will be documented in this file.

The format is based on Keep a Changelog, and this project uses semantic
versioning for release tags. This file is generated by `scripts/update-changelog.sh`.

## [Unreleased]

### Added

- Add safe configuration deletion workflows
- Add run lifecycle controls, deletes, and SFTP connection testing
- feat: sync grouped directories incrementally and reuse SFTP connections
- Add queue-item leasing, crash-recovery sweeps, and atomic run creation
- Add download retry budget with dead-lettering and resumable, verified transfers
- Add SSH host key pinning with trust-on-first-use verification (#8)
- Add SFTP end-to-end test harness and production ops hardening (#11)
- Add live download progress updates (#7)
- Add single-admin authentication for API, SPA, and SignalR hub (#9)

### Fixed

- fix: assign a unique trusted host key migration version
- fix(tests): use nullable run creation API
- fix: preserve SFTP authentication failures
- fix: reconcile run controls with incremental groups
- fix: guard grouped leaf downloads after cancellation
- fix: reconcile run controls after rebase
- fix: reconcile run controls with retry resume changes
- fix: renumber run lifecycle migration
- fix: keep runs cancelled when cancellation lands mid-scan
- fix: reconcile grouped sync migrations with main
- fix: skip missing failed queue items before requeue
- fix: discard .part file when remote changes during a failed download attempt

### Security

- Support SFTP authentication method transitions

### Documentation

- docs: update changelog [skip changelog]
- docs: update changelog [skip changelog]
- docs: limit pull request size
- docs: update changelog [skip changelog]
- docs: update changelog [skip changelog]
- docs: update changelog [skip changelog]
- docs: update changelog [skip changelog]
- docs: update changelog [skip changelog]

### Changed

- Verify trusted SFTP host keys
- Make sync run creation explicitly atomic
- Stop grouped downloads when run is cancelled
- Resolve retry resume rebase integration
- Resolve migration SQL scripts by convention from embedded resources (#14)

## [0.1.0] - 2026-06-04

### Added

- Add .idea/ to .gitignore
- Add queue item file deletion actions
- Add runtime DB call logging
- Add auto-queued SFTP sync worker
- Add folder grouping implementation plan
- Add repository functions and switch system property IDs to GUIDs
- Add managed encryption key initialization
- Add first-boot encryption key spec
- Add SPA proxy support for the web app
- Add Renovate config for grouped dependency updates
- Add UI admin pages and secret-preserving profile updates
- Add React frontend support and CI validation
- Add run and queue item query support for the UI
- Add CI pipeline and Postgres repository integration tests
- Add connection profile management and async job services

### Fixed

- Fix run aggregate recalculation query

### Security

- Sandbox local destination paths for jobs and downloads

### Documentation

- Set project license to MIT

### Tests

- Phase 4 complete: unit-testable FakeSftpDirectoryScanner + 7 tests proving first-child opaque grouping algorithm (interface from Phase 0)

### CI/CD

- Update CI/CD workflow and package-lock.json to rename references from SftpSync to SporeSync and adjust peer and libc configuration
- Publish Docker image only on tags
- Update package references to Microsoft.Extensions.* to version 10.0.8 and remove unused import in AdminPages.tsx
- Publish container images from CI on tags

### Changed

- Rename project branding to SporeSync
- Normalize embedded SQL resource prefixes
- Backfill system property functions
- Refactor SQL migrations to load ordered embedded scripts
- Handle nullable queue paths in dashboard
- Handle remote deletions during sync runs
- Replace hardcoded log level strings with `LogLevelConstants`
- Make DB logging components null-safe
- Centralize DB command logging in repositories
- Mark failed download groups and log scan errors
- Qualify status references in worker stats query
- Scope SFTP client factory per request
- Remove ESLint setup from client app
- Migrate client app to Tailwind 4 and Biome
- Phases 8+9 + full completion (all milestones M1-M4 delivered end-to-end)
- Phase 7 complete (minimal opaque folder group UI in dashboard)
- Phase 6 complete + M2 achieved (simulation can seed & drive groups + requeue demo)
- Phase 5 complete: requeue/progress semantics defined + broadcaster confirmed ready (M4)
- Phase 3 complete: backend models, DTOs, services, and run recalc for first-child opaque hybrid grouping
- Phase 2 complete: SQL functions updated for first-child opaque hybrid grouping (visible filter + internal leaves helper; run aggregates deferred)
- Phase 1 complete: schema migration for first-child opaque grouping columns (exact locked spec + subagent proposal)
- Phase 0 complete: locked first-child opaque grouping columns + rules + ISftpDirectoryScanner contract (exact per plan + subagent proposals)
- Update data annotations and configure JSON serialization options
- Switch web API docs to OpenAPI

